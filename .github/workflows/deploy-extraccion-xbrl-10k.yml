name: deploy-extraccion-xbrl-10k

on:
  push:
    branches: [main]
    paths:
      - "functions/extraccion-xbrl-10k/**"
      - ".github/workflows/deploy-extraccion-xbrl-10k.yml"
  workflow_dispatch:

concurrency:
  group: deploy-extraccion-xbrl-10k
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - id: deploy
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: extraccion-xbrl-10k
          region: us-east1
          source: functions/extraccion-xbrl-10k
          flags: --port=8080 --memory=2Gi --cpu=2 --concurrency=80 --service-account=846229407230-compute@developer.gserviceaccount.com --clear-base-image --timeout=900
          env_vars: |
            SEC_API_IDENTITY=${{ secrets.SEC_API_IDENTITY_10K }}