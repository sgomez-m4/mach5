name: deploy-extraccion-item2-10k

on:
  push:
    branches: [main]
    paths:
      - "functions/extraccion-item2-10k/**"
      - ".github/workflows/deploy-extraccion-item2-10k.yml"
  workflow_dispatch:

concurrency:
  group: deploy-extraccion-item2-10k
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - id: deploy
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: extraccion-item2-10k
          region: us-east1
          source: functions/extraccion-item2-10k
          flags: --port=8080 --memory=512Mi --cpu=1 --concurrency=80 --service-account=846229407230-compute@developer.gserviceaccount.com --clear-base-image
          env_vars: |
            FUNCTION_TARGET=procesar_bucket_edgar