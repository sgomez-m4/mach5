name: deploy-extraer-seccion-china

on:
  push:
    branches: [main]
    paths:
      - "functions/extraer-seccion-china/**"
      - ".github/workflows/deploy-extraer-seccion-china.yml"
  workflow_dispatch:

concurrency:
  group: deploy-extraer-seccion-china
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - id: deploy
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: extraer-seccion-china
          region: us-east1
          source: functions/extraer-seccion-china
          flags: --port=8080 --memory=512Mi --cpu=1 --concurrency=80 --service-account=846229407230-compute@developer.gserviceaccount.com --clear-base-image
          env_vars: |
            FUNCTION_TARGET=procesar_flota_aerolineas